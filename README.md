You can change the range in the Makefile
```
$START $PLUS $END
```

Execute all the version
```sh
make run
```

Plot all the performance
```sh
make plot
```

Execute one version only, the iteration is an example
```sh
make iteration
```
